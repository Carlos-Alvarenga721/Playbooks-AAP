all:
  children:
    linux_servers:
      hosts:
        web-server:
          ansible_host: 10.10.0.20
          ansible_user: ansible
          ansible_become: true
    windows_servers:
      hosts: {}